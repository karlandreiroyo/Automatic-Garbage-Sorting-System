# =============================================================================
# NutriBin — copy values into real .env files (do not commit secrets)
# =============================================================================

# --- YOLO / desktop (yolo/scripts/.env next to ws_url.py) ---------------------
# Switch modes without code changes:
MODE=local
# MODE=deployed

# Local: ML WebSocket server (node ws-server) host/port
WS_HOST=localhost
WS_PORT=3001

# Deployed: public WebSocket URL of your Railway *ws-server* service (wss://...)
# The main app URL (https://automatic-garbage-sorting-system-production.up.railway.app)
# is NOT a WebSocket endpoint unless you explicitly proxy WS there.
WS_URL=wss://YOUR-WS-SERVICE.up.railway.app

# Optional: if you only set the site URL, script maps https -> wss host (same host;
# only works if Railway exposes WebSocket on that hostname)
# NUTRIBIN_APP_URL=https://automatic-garbage-sorting-system-production.up.railway.app

# --- Frontend (frontend/.env) — rebuild after changes -----------------------
VITE_API_URL=http://localhost:3001
# Railway backend HTTPS URL:
# VITE_API_URL=https://YOUR-BACKEND.up.railway.app

VITE_MODE=local
# VITE_MODE=deployed

# Required on deployed when ML ws-server has its own Railway URL:
VITE_DEPLOYED_WS_URL=wss://YOUR-WS-SERVICE.up.railway.app

# Or set a single explicit WS URL (overrides MODE for WS):
# VITE_WS_URL=wss://YOUR-WS-SERVICE.up.railway.app

VITE_LOCAL_WS_PORT=3001

# --- Backend (backend/.env) -------------------------------------------------
PORT=3001
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
FRONTEND_URL=http://localhost:5173
# FRONTEND_URL=https://automatic-garbage-sorting-system-production.up.railway.app

# --- ws-server Railway service ------------------------------------------------
PORT=3001
# Railway sets PORT automatically; no code change needed.

# --- Run locally --------------------------------------------------------------
# Terminal 1 — backend:  cd backend && npm start
# Terminal 2 — ws ML:    cd ws-server && npm start   (or from root: npm run ws-server)
# Terminal 3 — frontend: cd frontend && npm run dev
# YOLO: set MODE=local, run your script (must send JSON detection to ws-server)
