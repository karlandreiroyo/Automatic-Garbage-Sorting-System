/**
 * Shared routes (auth, health - used by all roles).
 * These are required individually in server.js and mounted at:
 *   /api/forgot-password, /api/profile, /api/login, /api/health, /api/security
 */
