# Deployed: Railway ML WebSocket (browsers on Railway use the same URL).
WS_URL=wss://ws-server-production-ab05.up.railway.app

# Local dev: comment the line above and use:
# WS_URL=ws://localhost:3001
