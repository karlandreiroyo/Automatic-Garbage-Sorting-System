/**
 * Superadmin uses the same dashboard shell and inner pages as admin.
 * Kept as a separate entry path for imports; behavior matches AdminDashboard.
 */
export { default } from '../admin/admindashboard';
